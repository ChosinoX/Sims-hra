'use client';import{useState}from'react';import{useRouter}from'next/navigation';
export default function Home(){const r=useRouter(),[register,setRegister]=useState(false),[error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch(`/api/auth/${register?'register':'login'}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password'),remember:!!f.get('remember')})});if(res.ok)r.push('/game');else setError('Přihlášení se nezdařilo / Sign in failed')}return <main className="landing"><section className="hero"><div className="logo"><span>✦</span> LumaLife</div><h1>Tvůj život. Tvé tempo.<br/><em>Tvůj zářivý příběh.</em></h1><p>Vybuduj domov, rozvíjej talent, poznej sousedy a prožij každý den po svém.</p><div className="features"><b>🏡 Vlastní domov</b><b>🌆 Živé město</b><b>🤝 Přátelé</b></div></section><form className="login" onSubmit={submit}><h2>{register?'Vytvořit účet':'Vítej zpět'}</h2><p>{register?'Začni nový příběh':'Pokračuj ve svém příběhu'}</p><label>E-mail<input name="email" type="email" required/></label><label>Heslo<input name="password" type="password" minLength={register?10:1} required/></label><label className="check"><input name="remember" type="checkbox"/> Zapamatovat si mě</label>{error&&<div className="error">{error}</div>}<button>{register?'Registrovat':'Vstoupit do LumaLife'}</button><button className="link" type="button" onClick={()=>setRegister(!register)}>{register?'Už mám účet':'Nemáš účet? Zaregistruj se'}</button><small>Registrací souhlasíš s <a href="/legal">podmínkami a soukromím</a>.</small></form></main>}
